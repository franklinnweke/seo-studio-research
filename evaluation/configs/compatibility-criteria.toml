schema_version = 1
criteria_id = "compatibility-v1"
purpose = "Configuration eligibility only; this stage must not select a quality winner."
pilot_items = 20
warmup_manifest = "dataset/manifest-contract.jsonl"
warmup_image_id = "synthetic-solid-blue-001"
minimum_schema_valid_rate = 0.95
per_attempt_timeout_seconds = 240
hidden_retries_allowed = 0
abort_on_transport_error = true
max_transport_attempts_per_item = 3
temperature = 0
seed = 20260716
thinking_mode = "disabled"
output_token_limit = 384
cold_smoke_keep_alive = 0
pilot_keep_alive = "10m"
pilot_warm_up_attempts_per_model = 1
required_input_modalities = ["text", "image"]
required_evidence = ["license", "exact_ollama_tag", "full_digest", "parameter_size", "quantization", "ollama_version", "structured_output_validity", "native_timing"]
incompatible_replacement_reasons = ["runtime_version", "arm64_support", "license_restriction", "vision_input_failure", "structured_output_failure", "timeout"]
replacement_quality_inspection_allowed = false

[advancement]
hard_eligibility_order = ["license", "compatibility", "structured_output_validity", "safety_threshold"]
ranking_order = ["supported_claim_precision", "hallucinated_claim_performance", "quality_reliability_latency_pareto"]
tie_break_order = ["failure_rate", "median_warm_latency"]
finalist_rule = "current baseline plus the two highest-ranked eligible non-baseline challengers; three unique finalists"
